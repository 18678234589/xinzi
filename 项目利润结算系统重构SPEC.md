# 项目利润结算与工资中心重构 Spec（V1）

## 1. 文档目的

将现有“员工订单金额 × 个人算法”的薪资系统，扩展为以**项目订单**为核心的利润核算和项目提成系统。V1 的目标是让销售、客服、技术、财务在各自职责内完成录入与审核，系统自动完成项目直接成本、项目贡献利润、技术与客服分别返佣、多人提成分配和月度工资汇总。

本 Spec 以 `订单模板/AI网站定制填写模板.xlsx` 为现有订单来源。模板中的日期、店铺、业务、付款昵称、订单编号、售价、完成状态、客户联系方式、客服、前端、域名使用、SSL 实际成本、后端等字段继续可导入，但不再把 Excel 当作成本和提成的唯一来源。

### 1.1 业务原则

1. 一张订单只保留一个唯一订单号，所有收入、退款、成本、参与人和审核记录均归属该订单。
2. 技术可发起草稿订单并报备售价、资源使用和特殊成本，但不能确认实收、修改已核定成交金额、退款、标准成本单价或项目分成比例；财务确认收入。
3. 标准成本从成本模板库带入，技术不能改价；自定义成本必须填写原因并上传凭证。
4. 提成只以“项目贡献利润”为基础：可结算收入 − 项目直接成本。
5. 房租、行政工资、水电等间接成本不分摊到订单，不参与技术订单提成。
6. 任何已经审核或进入工资结算的金额都保留计算快照和操作日志，不能静默覆盖历史。
7. 客服也按项目贡献利润的约定百分比返佣；客服提成与技术提成分别计提、分别分配，最后汇入各自的月度工资。

### 1.2 非目标

- V1 不做完整 ERP、采购付款、银行代发或会计总账。
- V1 不自动从云厂商扣费账单中识别成本；先以模板成本与人工凭证为准。
- V1 不让技术自行改变提成规则或结算状态。

## 2. 角色与权限

| 角色 | 可操作 | 禁止操作 |
| --- | --- | --- |
| 销售/客服 | 创建或导入本人负责的订单、填写成交信息、录入收款与退款申请、维护客户和交付负责人；查看本人参与订单的利润及客服提成、本人月度工资 | 查看无关员工的订单和工资；修改标准成本、提成规则、已审核成本、工资结算 |
| 技术 | 在被分配订单中添加标准成本、提交特殊成本与凭证、确认资源周期、查看本人参与订单及本人预计提成和工资 | 查看无关员工的订单和工资；修改订单金额、退款、标准单价、他人权重、审核状态 |
| 项目负责人 | 维护项目参与人和权重、确认项目完工、提交项目结算 | 修改已锁定规则和已审核财务数据 |
| 财务 | 审核特殊成本与退款、维护成本模板、配置提成规则、生成/锁定工资结算、导出报表 | 修改技术的实际使用记录而不留痕 |
| 管理员 | 用户、角色、阈值、规则版本、历史修正授权 | 无业务限制，但所有修正必须记录日志 |

权限必须在服务端校验，前端隐藏按钮不视为权限控制。

## 3. 统一名词与计算口径

| 名词 | 定义 | 计算/规则 |
| --- | --- | --- |
| 订单合同金额 | 销售确认的成交金额 | 只读业务字段，可有变更记录 |
| 实收金额 | 已到账收款之和 | 由收款记录累计 |
| 已审核退款 | 财务审核通过的退款之和 | 未审核退款不影响正式利润 |
| 可结算收入 | 用于项目提成的收入 | 默认 `实收金额 - 已审核退款`；未到账项目不可正式结算 |
| 项目直接成本 | 为该订单直接发生、可追溯的资源/外包成本 | 已审核或自动通过的成本明细之和 |
| 项目贡献利润 | 用于计算项目提成的利润 | `可结算收入 - 项目直接成本` |
| 技术提成池 | 项目可分给技术组的总提成 | `max(项目贡献利润, 0) × 技术组提成比例` |
| 客服提成池 | 项目可分给客服组的总返佣 | `max(项目贡献利润, 0) × 客服组返佣比例` |
| 个人提成 | 参与人从所属组提成池获得的金额 | `所属组提成池 × 本人在该组的分配权重` |
| 客服绩效 | 现有绩效表产生的项目报酬项目 | 单独列示；已确认与新增客服项目分成叠加 |
| 结算快照 | 某次审核/锁定时所有参与计算的值 | 后续价格、规则改变不回写历史 |

金额以 `DECIMAL(14,2)` 存储，比例以 `DECIMAL(7,6)` 存储。收入和成本在订单层只计算一次，技术、客服使用同一项目贡献利润分别计算提成池，不得把一组的返佣当作另一组的成本再次扣除。各组提成池与个人提成均按两位小数四舍五入；组内分摊的末位差额归入该组权重最高者，并在明细中标明“舍入调整”。

## 4. 核心用户流程

### 4.1 销售/客服：导入或创建订单

1. 上传现有 AI 网站定制模板或手工创建订单。
2. 系统按“订单编号”去重；重复导入显示“新建、更新草稿、冲突”三类结果，不允许直接覆盖已审核订单。
3. 映射字段：`订单编号 → order_no`，`售价 → contract_amount`，`状态 → delivery_status`，`客服/前端/后端 → 初始参与人建议`。
4. 销售录入或确认客户、订单业务类型、合同金额、实收金额、负责人和项目状态。
5. 订单进入“待技术补成本”状态。

### 4.2 技术：一张订单结算单完成成本录入

技术打开订单后只看到一张结算单，不进入传统多字段 ERP 表单：

```text
订单：202609220018        状态：待成本审核
客户：XX公司              技术负责人：张三
可结算收入：¥6,800.00     该字段由销售/财务维护，技术只读

成本明细
类型      项目              数量/周期    单价       小计       状态
域名      .com 域名         1 年        ¥75.00     ¥75.00     自动通过
服务器    基础 VPS 2核4G    1 年        ¥480.00    ¥480.00    自动通过
认证      微信认证          1 次        ¥300.00    ¥300.00    待财务审核
...

可结算收入 ¥6,800.00 - 直接成本 ¥1,215.00 = 项目贡献利润 ¥5,585.00
技术提成池 ¥670.20（利润 ¥5,585.00 × 12%）
客服提成池按该订单适用的客服返佣比例另行计算
```

点击“添加项目成本”后，按类别选择：域名、服务器、认证、API、插件、外包、其他。选择路径为“类别 → 模板项 → 规格/周期 → 数量”；系统自动填充单价、计价单位、成本类型和是否需凭证。录入后立即重算顶部四个数字。

### 4.3 成本模板库

管理员维护模板及价格版本，例如：

| 类别 | 模板项 | 规格/周期 | 标准价 | 凭证规则 | 成本类型 |
| --- | --- | --- | ---: | --- | --- |
| 域名 | .com 域名 | 1 年 | 75.00 | 可选 | 年度成本 |
| 域名 | .cn 域名 | 1 年 | 35.00 | 可选 | 年度成本 |
| 服务器 | 基础 VPS | 2核4G/1年 | 480.00 | 可选 | 年度成本 |
| 认证 | 微信认证 | 1 次 | 300.00 | 必填 | 一次性成本 |
| API | 短信充值 | 1 包 | 200.00 | 必填 | 一次性成本 |

模板价格不得直接覆盖旧值。修改价格时新增版本并设置生效日期；订单成本行保存 `template_version_id`、单价快照和项目名称快照。

自定义成本规则：

- 技术必须填写项目名称、金额、原因、成本类型和付款凭证。
- 金额超过配置阈值（V1 默认 ¥500）或类别为“其他/外包/自定义”时，自动进入待财务审核。
- 未审核成本可显示在“预计利润”中，但不计入“可发放提成”；页面必须同时清晰显示预计与已审核金额。

### 4.4 多人参与与分润

每个订单维护参与人列表，字段为员工、提成组（技术/客服）、岗位、组内权重、生效状态。默认可从 Excel 的前端/后端/客服列生成建议参与人，但项目负责人必须确认。订单有技术和客服两套独立的利润返佣比例；两组比例可以不同，且都由财务维护的版本化规则决定。

规则：

- 按提成组分别校验：有技术参与人时，技术组权重之和必须等于 100%；有客服参与人时，客服组权重之和必须等于 100%。技术 100% 加客服 100% 是合法配置。
- 可配置岗位，如后端、前端、部署、客服；岗位用于展示和默认权重，实际金额由所属组比例和组内权重决定。
- 技术组提成比例与客服组返佣比例分别匹配“项目提成规则”，按业务类型、部门、订单标签等条件匹配，并保存各自命中的规则版本。某组无参与人时，不生成该组提成；有参与人却没有有效比例时，阻止提交并提示财务配置。
- 同一员工如同时承担技术和客服职责，须在两组分别登记，工资按两条有来源的提成记录汇总；同一组内同一员工只允许一条有效参与记录。
- 当贡献利润小于等于 0 时，两组提成池均为 0；禁止产生负提成，除非财务通过“负向调整单”单独处理。

示例：贡献利润 ¥8,000，技术组比例 12%、客服组比例 3%。技术提成池 ¥960；后端 50%、前端 30%、部署 20%，分别获得 ¥480、¥288、¥192。客服提成池 ¥240；若只有一名客服，其权重 100%，获得 ¥240。订单总项目提成 ¥1,200，两组均基于同一笔 ¥8,000 利润计算。

### 4.5 财务审核与锁定

订单状态流转：

```text
草稿 → 待补收款 / 待成本录入 → 待财务审核 → 待项目确认
→ 可纳入工资 → 已锁定结算 → 已发放
```

财务审核页按风险集中处理：自定义成本、超阈值成本、缺凭证成本、退款、利润异常、技术/客服任一组权重不等于 100%、任一参与组缺少提成比例、收入未到账。审核动作包含“通过、驳回、退回补充”，必须留审核意见。

订单进入“已锁定结算”后，收入、成本、规则、参与人不允许普通编辑。发生退款、成本补录或更正时，创建“结算调整单”，在当前或指定工资月生成正/负调整，保留原结算单不变。

### 4.6 员工工资结算中心

技术、客服均可按月份查看本人参与的订单和工资组成，不能查看其他员工的订单与工资。财务可选择月份、部门、员工，查看某位技术或客服该月全部工资，并可汇总全员。每个订单提成可展开至收入、退款、每条成本、审核状态、利润、所属提成组、组内比例、权重和个人提成。

```text
张三｜2026年9月
订单号       提成组  可结算收入   直接成本   贡献利润   组比例   组内权重    本人提成
202609220018  技术    6,800.00    1,215.00   5,585.00   12%      100%       670.20
202609220031  技术   12,800.00    2,400.00  10,400.00   12%       50%       624.00

底薪 6,000.00 + 项目提成 1,294.20 + 奖金 300.00 - 扣款 0.00 = 应发 7,594.20
```

工资状态：草稿、待复核、已复核、已锁定、已发放。锁定后只允许通过调整单改变后续工资。

客服工资页使用同一结构，提成组显示“客服”，比例显示该订单的客服返佣比例。财务进入工资中心默认选择上一个自然月，并可切换到任何开放或历史月份；员工选择器显示姓名、部门和员工编号以区分重名。

### 4.7 自动续费

订单成本按 `one_time`、`annual`、`monthly` 分类。年度/月度成本可填写开始日期、下次续费日期、是否自动续费、责任人和客户归属。

- 系统在续费日前 30/15/7 天产生待办。
- 确认续费后创建独立“续费订单/续费成本记录”，引用原项目订单。
- 续费收入与续费成本计入续费订单，不回写首次项目的贡献利润。
- 未确认的续费只预警，不自动生成实际成本或技术、客服提成。

## 5. 页面与交互规格

### 5.1 月度结算工作台（财务首页）

顶部选择月份，显示：待补收入、待审核成本、待处理退款、可结算订单、未结算员工、预计应发、已锁定应发。下方按“阻塞项优先”列出可点击清单。

操作主线固定为：`导入订单 → 补齐成本 → 审核异常 → 批量试算 → 复核锁定 → 导出发放`。

### 5.2 订单列表

筛选：月份、业务类型、项目状态、成本状态、审核状态、参与人、客户、订单号、是否续费。列必须包含订单号、客户、可结算收入、直接成本、贡献利润、技术提成池、客服提成池、状态、最后操作人/时间。普通员工列表由服务端按本人参与或负责的订单限制；财务可查看全部订单。

颜色只用于状态辅助，所有状态同时以文本标签显示。利润异常规则建议为：负利润、成本率超过 60%、成本大于实收、超过 30 天未补成本。

### 5.3 订单结算单

页面按“订单基本信息、实时利润摘要、成本明细、参与人、审核记录”从上至下排列。成本操作仅保留一个主按钮“添加项目成本”。

必须避免：把标准成本、自定义成本、财务审核、工资结算混在一个没有状态说明的长表单中。

### 5.4 成本模板库

支持分类树、搜索、启用/停用、价格版本、凭证规则、默认周期、审核阈值、适用业务类型。停用模板不影响历史订单，也不可在新订单选择。

### 5.5 工资结算中心

财务视图提供月份（默认上月）、部门、员工、岗位、状态筛选；选定某一技术或客服后，展示该员工在所选月份的全部订单提成、底薪、奖金、扣款、调整和应发工资。支持批量试算、锁定、导出银行表和工资条。员工视图只呈现本人订单、本人提成和工资计算明细，不展示其他员工、规则配置或财务内部备注；所有列表、详情、接口和导出均执行相同的数据范围校验。

## 6. 数据模型

现有 `orders` 表不能直接承担新模型：它目前以 `employee_id + order_amount + order_date` 为核心，缺少订单号、客户、收款、成本、多人参与、审批和快照。建议保留其作为旧订单来源，新增下列表；迁移期间以新表为结算唯一来源。

### 6.1 主数据

| 表 | 关键字段 |
| --- | --- |
| `users` | id, username, password_hash, role, employee_id, status |
| `employees`（扩展） | employee_no, name, department_id, employment_status, base_salary |
| `customers` | id, customer_name, contact_name, contact_phone, contact_wechat |
| `project_types` | id, name, commission_rule_scope, enabled |
| `cost_categories` | id, name, sort, enabled |
| `cost_templates` | id, category_id, name, specification, unit, cost_kind, requires_proof, enabled |
| `cost_template_versions` | id, template_id, unit_price, effective_from, effective_to, created_by |
| `commission_rules` | id, commission_group（technical/customer_service）, scope_type, scope_id, rate, effective_from, effective_to, priority, enabled |

### 6.2 订单与结算数据

| 表 | 关键字段 |
| --- | --- |
| `project_orders` | id, order_no, customer_id, project_type_id, contract_amount, delivery_status, settlement_status, source_order_id, created_by |
| `order_receipts` | id, order_id, receipt_amount, receipt_date, channel, status, evidence_url |
| `order_refunds` | id, order_id, refund_amount, refund_date, reason, status, reviewed_by |
| `order_costs` | id, order_id, template_id, template_version_id, item_name_snapshot, qty, unit_price_snapshot, amount, cost_kind, start_date, next_renewal_date, status, proof_url, submitted_by, reviewed_by |
| `order_participants` | id, order_id, employee_id, commission_group（technical/customer_service）, role, group_weight, status, confirmed_by |
| `order_profit_snapshots` | id, order_id, snapshot_type, income_amount, direct_cost_amount, contribution_profit, technical_rate, technical_pool, customer_service_rate, customer_service_pool, created_at |
| `order_commissions` | id, order_id, employee_id, snapshot_id, commission_group, group_rate_snapshot, group_weight_snapshot, commission_amount, rule_id, rule_version_snapshot, status |
| `payroll_periods` | id, period, status, calculated_at, locked_at, paid_at |
| `payroll_items` | id, payroll_period_id, employee_id, base_salary, order_commission, bonus, deduction, adjustment, net_pay, status |
| `settlement_adjustments` | id, source_order_id, target_period, adjustment_type, amount, reason, status, created_by, reviewed_by |
| `audit_logs` | id, entity_type, entity_id, action, before_json, after_json, actor_id, created_at |

关键索引：`project_orders.order_no` 唯一；`order_costs(order_id, status)`；`order_participants(order_id, commission_group, employee_id)` 唯一有效参与记录；`order_commissions(employee_id, status)`；`payroll_items(payroll_period_id, employee_id)` 唯一；`audit_logs(entity_type, entity_id, created_at)`。

## 7. 服务端规则与校验

1. 所有金额、提成、状态计算在 PHP 服务端执行；浏览器的实时预览仅用于展示。
2. 订单号唯一；导入时重复订单号必须进入冲突列表。
3. 收款、退款、成本、参与人任一变更后，订单标记为“需重新试算”。
4. 标准成本行的单价由成本模板版本写入快照；接口不接受技术传来的替换单价。
5. 自定义成本必须有原因；要求凭证的成本在上传成功前不能提交审核。
6. 对每个有参与人的提成组分别校验：组内有效权重总和必须精确等于 1.000000；不满足时禁用“提交结算”。两组间不汇总权重。
7. 只有已审核收入、退款和成本进入正式快照；预计数字与正式数字使用不同字段和标签。
8. 已锁定工资期不允许重算覆盖；必须创建调整单。
9. 删除采用软删除或作废状态，禁止物理删除已审核、已锁定、已发放记录。
10. 所有审核、锁定、驳回、作废和调整必须写入 `audit_logs`。
11. 订单详情、月度工资、搜索结果和导出均按登录用户的员工 ID 做服务端范围限制；财务角色可按月份查看任意技术或客服的完整工资。

## 8. 接口草案

| 方法 | 地址 | 用途 |
| --- | --- | --- |
| POST | `/api/orders/import` | 导入 AI 网站定制模板，返回字段映射与冲突结果 |
| GET | `/api/orders/{id}/settlement-sheet` | 获取订单结算单、权限和实时预览 |
| POST | `/api/orders/{id}/costs` | 技术添加标准/自定义成本 |
| POST | `/api/order-costs/{id}/submit-review` | 提交特殊成本审核 |
| POST | `/api/order-costs/{id}/review` | 财务通过/驳回成本 |
| PUT | `/api/orders/{id}/participants` | 保存参与人与权重 |
| POST | `/api/orders/{id}/recalculate` | 生成预计或正式利润快照 |
| POST | `/api/orders/{id}/submit-settlement` | 项目负责人提交财务审核 |
| POST | `/api/payroll-periods/{period}/calculate` | 按月批量试算 |
| POST | `/api/payroll-periods/{period}/lock` | 财务锁定工资期 |
| GET | `/api/me/orders?period=YYYY-MM` | 技术或客服查看本人负责/参与订单 |
| GET | `/api/me/payroll?period=YYYY-MM` | 技术或客服查看本人结算明细 |
| GET | `/api/payroll?period=YYYY-MM&employee_id=ID` | 财务查看指定员工该月全部工资及订单明细 |

所有写接口返回：实体当前状态、版本号、计算摘要、可见错误字段和操作日志编号。使用乐观锁 `row_version` 防止多人编辑时后提交者覆盖前一人的更改。

## 9. 导入模板兼容方案

| Excel 列 | 导入目标 | 处理规则 |
| --- | --- | --- |
| 日期 | `project_orders.created_at/source_date` | 保留原始日期 |
| 店铺 | 订单来源/渠道 | 自动匹配或创建待确认值 |
| 业务 | `project_type` | 映射到项目类型，如 AI 网站定制 |
| 付款昵称 | 订单付款信息 | 仅财务可见，技术隐藏 |
| 订单编号 | `order_no` | 必填且唯一 |
| 售价 | 合同金额 | 导入为待销售确认，不等同实收 |
| 状态 | 交付状态 | 仅接受已完成/未完成，其他进入错误清单 |
| 备注 | 客户联系方式/备注 | 仅本单参与人和财务可见 |
| 客服/前端/后端 | 初始参与人建议 | 映射员工后等待负责人确认权重 |
| 域名使用 | 资源选择 | “否/无需域名”不生成成本；“是”且域名后缀与唯一的年度 1 年模板匹配时预选该模板，导入确认后按成本库现行价格生成成本快照；不能唯一识别时在预览页选择，禁止默认为 .com |
| SSL 证书使用 | 待补成本要求 | 正数金额记录为 SSL 待补录要求；技术上传凭证并由财务审核相应证书成本前，订单不得审核生成分成 |
| 域名或空间 | 资源提示 | 可用于识别域名后缀；无法确认具体服务器规格时仅作提示，预览页可另选服务器模板，不猜价格 |

导入必须提供预览页，显示：有效行、缺订单号、重复订单号、合作人员未匹配、金额格式错误、状态值错误及待选择域名模板。技术/客服只可导入本人参与行。只有确认后才写库；未明确域名选择的行跳过，不会默默写入零成本。手动录入提供相同的域名/服务器成本模板选择。模板价格由财务配置并在成本明细保存版本快照，示例 ¥75 不自动成为真实采购价。

## 10. 验收标准

### 10.1 成本与利润

- 技术选择“服务器 → 基础 VPS → 2核4G/1年”后，系统以模板版本的 ¥480 自动生成成本行，技术无法改价。
- 技术添加 ¥600 的自定义 API 成本且未上传凭证时，不能提交；上传后显示“待财务审核”。
- 已审核收入 ¥6,800、直接成本 ¥1,215 时，订单贡献利润必须为 ¥5,585。
- 贡献利润 ¥5,585、技术比例 12%、技术组内权重 100% 时，技术个人提成必须为 ¥670.20。

### 10.2 多人分润

- 技术组权重为 50%、30%、20% 时系统允许提交，三人提成合计等于技术提成池。
- 同一订单贡献利润 ¥8,000、技术比例 12%、客服比例 3% 时，技术提成池必须为 ¥960，客服提成池必须为 ¥240；客服一人权重 100% 时获得 ¥240，技术组的 ¥960 不变。
- 任一组权重合计为 99% 或 101% 时系统指出具体组别和差额，禁止提交；技术组 100% 加客服组 100% 时允许提交。
- 客服返佣比例修改后，新订单采用新版本；已锁定订单保留原比例和提成金额。

### 10.3 权限与追溯

- 技术或客服接口尝试更新订单合同金额、标准成本单价、提成规则时返回 403。
- 技术或客服只能获取本人负责/参与订单与本人月度工资；直接访问其他员工的订单详情、工资接口和导出均被拒绝。
- 财务选择 2026-09 和指定客服/技术时，可看到该员工当月所有订单提成和完整工资汇总；默认月份为上一个自然月，重名员工显示部门及员工编号。
- 财务通过成本、锁定工资、创建调整单后，审计日志可查询到人员、时间、前后值和原因。

### 10.4 锁定与调整

- 已锁定的 2026-09 工资不能被重新计算覆盖。
- 对已锁定订单新增退款后，系统提示创建调整单；调整单审批通过后，仅影响目标工资期并可追溯来源订单。

## 11. 分期实施计划

### Phase 0：基础治理（1 周）

补齐员工编号、部门 ID、角色账户；清理重名员工映射；确定成本类别、标准价格、审核阈值和提成规则。冻结现有薪资数据，仅迁移必要订单。

### Phase 1：订单利润核算 MVP（2–3 周）

实现项目订单、Excel 导入预览、成本模板库、订单结算单、标准/自定义成本、财务审核、贡献利润实时计算、审计日志。此阶段支持单人技术提成及单人客服按利润返佣，并实现两类员工仅查看本人订单和预计提成的服务端权限。

### Phase 2：多人分润与工资中心（2 周）

实现技术/客服各组多人权重、独立规则匹配和提成池、个人提成明细、月度批量试算、财务按月按人查询完整工资、员工工资结算中心、锁定和导出。

### Phase 3：续费与精细化运营（1–2 周）

实现年度/月度成本、续费提醒、续费订单、调整单、利润异常看板和管理报表。

## 12. 对现有系统的改造建议

1. 保留现有考勤、保险、客服绩效和固定服务费模块，作为项目报酬结算的其他组成部分。原有客服绩效与新增客服项目分成叠加；技术同一订单只保留新利润分成，须逐单核销旧分成。
2. 新增“项目利润”一级菜单，不直接把该功能塞进当前“订单上传”页面。
3. 当前 `orders` 旧数据先映射为历史只读订单；新项目从 `project_orders` 开始，不要继续在旧表上不断加 JSON 字段。
4. 现有 `SalaryCalculator` 保留处理底薪、考勤、绩效、保险；新增 `ProjectProfitCalculator` 专门处理收入、成本、利润和项目提成，避免一个计算器承担所有业务规则。
5. 先上线“订单结算单 + 成本模板库 + 审核”，确认技术录入习惯后，再批量切换工资结算口径。

## 13. 上线前需业务确认的参数

1. 提成使用实收口径，还是“已完成且已收款”口径；建议仅以已审核实收减已审核退款计算。
2. 未收齐尾款的项目是否允许按已收部分先结算；建议默认不允许，特殊项目由财务审批。
3. 模板成本是否一律自动通过，还是认证/API/外包仍必须附凭证。
4. 超额成本阈值是否统一 ¥500，或按成本类别分别配置。
5. 项目提成规则优先级：项目专属 > 项目类型 > 部门/角色 > 员工默认 > 系统默认。
6. 退款、补成本发生在工资已发放后，调整计入当月还是下月；建议默认计入下一未锁定工资期。
7. 已确认：现有客服绩效与新增“项目贡献利润 × 客服项目分成比例”叠加，在结算单上分别列示；不得把原系统净额内含的奖扣再次相加。

## 14. 已确认的界面术语与结算口径（2026-09-22）

界面统一使用“项目合作结算中心”；“工资结算”改为“项目结算”，“员工”改为“合作人员”，“工资”改为“项目报酬”，“提成”改为“项目分成”，“应发工资”改为“应结算金额”，“工资单/工资条”改为“结算单”，“底薪”改为“固定服务费”，“奖金”改为“项目奖励”，“发工资”改为“发放项目报酬”。数据库字段、历史记录和计算接口名称保持不变，避免破坏兼容性。

财务按月按人查看时，试算口径为：原系统已结算金额（`salaries.net_pay`，内部已包含原有客服绩效、考勤、奖扣及保险）－经财务逐单核实的旧技术同单分成＋新审核的技术/客服项目分成快照＝预计应结算金额。客服原有绩效与新客服分成叠加；技术同一订单只保留新利润分成。旧 `salaries.commission` 为多模块月度混合汇总，不能直接视为旧技术分成或按新分成倒推；财务须对每条技术快照填写旧分成金额、旧结算月份和核对依据，确认旧系统未计提则录入零元及原因。存在未核对技术快照时，不显示完整应结算金额，也不允许锁定该月。试算不自动改写原系统薪资记录，也不代表已经发放。
